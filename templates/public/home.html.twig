{% extends 'base.html.twig' %}

{% block title %}Learn in Project - Accueil{% endblock %}

{% block body %}
    <h1 class="text-center my-4">Homepage</h1>
    <div class="container d-flex flex-column align-items-center">
        <div class="mt-3">
            <a class="btn btn-primary" href="{{ path("school_signin") }}">Inscription école</a>
        </div>
        <div class="mt-3">
            <a class="btn btn-primary" href="{{ path("company_signin") }}">Inscription entreprise</a>
        </div>
        <div class="mt-3">
            <a class="btn btn-primary" href="{{ path("app_login") }}">Connexion</a>
        </div>

    </div>

{% endblock %}