{% extends 'base.html.twig' %}

{% block body %}
    <div class="signin">
        <div class="left-side">
            <img class="mt-4 d-block mx-auto" src="{{ asset("/public/images/logo-lip.svg") }}" alt="Logo LIP">
        </div>
        <div class="p-5 right-side">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1>Inscription <br />
                        Entreprise</h1>
                </div>
                <div class="w-75">
                    Nous avons besoin de vous pour nous fournir des renseignements de base sur la création d'un compte. Voici nos conditions d'utilisation. Veuillez les lire attentivement. Nous sommes conformes au RGPD
                </div>
            </div>
            <img class="my-5 d-block mx-auto w-100" src="{{ asset("/public/images/separator.svg") }}" alt="Séparateur">
            {{ form_start(form) }}
            <div class="row">
                <div class="col-lg-6">
                    {{ form_row(form.email) }}
                </div>
                <div class="col-lg-6">
                    {{ form_row(form.first_name) }}
                </div>
            </div>
            <div class="row mt-5">
                <div class="col-lg-6">
                    {{ form_row(form.last_name) }}
                </div>
                <div class="col-lg-6">
                    {{ form_row(form.company) }}
                </div>
            </div>
            <img class="my-5 d-block mx-auto w-100" src="{{ asset("/public/images/separator.svg") }}" alt="Séparateur">
            <div class="row">
                <div class="col-lg-6">
                    {{ form_row(form.siret) }}
                </div>
                <div class="col-lg-6">
                    {{ form_row(form.password) }}
                </div>
            </div>
            <button type="submit" class="btn btn-primary mt-4">Inscription</button>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}